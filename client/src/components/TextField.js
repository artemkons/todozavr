import React from "react";

const TextField = () => {
  return (
    <textarea
      className="text"
      value="Введите описание!"
      className="form-input todo-text"
    ></textarea>
  );
};

export default TextField;
